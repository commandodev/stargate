[buildout]
parts = install test cover-test
develop = .

[install]
recipe = gp.recipe.pip
eggs =
    rpz.websocket
editables =
    svn+http://svn.repoze.org/repoze.bfg/trunk/#egg=repoze.bfg
    hg+http://bitbucket.org/which_linden/eventlet/#egg=eventlet
interpreter = py

[test]
recipe = pbp.recipe.noserunner
eggs =
       rpz.websocket
       nose
       coverage
       mock==0.6.0
script = test
defaults = -s -v --with-doctest --doctest-extension=rst

[cover-test]
<= test
script = cover-test
defaults = ${test:defaults} --with-coverage --cover-package=rpz.websocket --cover-html --with-xunit